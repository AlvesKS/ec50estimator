% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ec50-workflow.R
\name{residual_data}
\alias{residual_data}
\alias{plot_residuals}
\title{Extract and plot EC50 residual diagnostics}
\usage{
residual_data(x, models = "all")

plot_residuals(x, models = "all", type = c("fitted", "dose"))
}
\arguments{
\item{x}{An object returned by [estimate_EC50()] or [ec50_multimodel()].}

\item{models}{One of `"all"`, `"best"`, or a character vector of model names.}

\item{type}{For `plot_residuals()`, plot residuals against fitted values or
dose.}
}
\value{
`residual_data()` returns a data frame; `plot_residuals()` returns a
  `ggplot2` object.
}
\description{
`residual_data()` returns observed, fitted, and residual values from stored
models. `plot_residuals()` returns a `ggplot2` diagnostic plot.
}
\examples{
data(multi_isolate)
fit <- estimate_EC50(
  growth ~ dose,
  data = subset(multi_isolate, isolate == 1 & fungicida == "Fungicide A"),
  isolate_col = "isolate",
  fct = drc::LL.3()
)
residual_data(fit)
plot_residuals(fit)

}
