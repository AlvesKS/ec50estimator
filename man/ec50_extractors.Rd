% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ec50-extractors.R
\name{ec50_extractors}
\alias{ec50_extractors}
\alias{ec50_estimates}
\alias{ec50_metadata}
\alias{fitted_models}
\alias{curve_data}
\title{Extract information from fitted EC50 objects}
\usage{
ec50_estimates(x)

ec50_metadata(x)

fitted_models(x)

curve_data(x, n_points = 200, log_x = TRUE, quiet = FALSE)
}
\arguments{
\item{x}{An object returned by [estimate_EC50()] or [ec50_multimodel()].}

\item{n_points}{Number of dose values used to draw each fitted curve.}

\item{log_x}{Logical. If `TRUE`, curve coordinates are generated only over
positive dose values for log10 x-axis plotting.}

\item{quiet}{Logical. If `FALSE`, failed curve predictions are reported with a
warning.}
}
\value{
`ec50_estimates()` returns a plain data frame of EC estimates.
`ec50_metadata()` returns a list with modeling metadata.
`fitted_models()` returns a named list of stored `drc` model objects.
`curve_data()` returns a data frame of fitted curve coordinates.
}
\description{
These helpers make fitted objects from [estimate_EC50()] and
[ec50_multimodel()] easier to reuse. They return plain data frames, metadata,
stored `drc` models, or fitted curve coordinates without requiring users to
work with object attributes directly.
}
\examples{
data(multi_isolate)
sample_data <- subset(
  multi_isolate,
  isolate \%in\% 1:3 & fungicida == "Fungicide A"
)

fit <- estimate_EC50(
  growth ~ dose,
  data = sample_data,
  isolate_col = "isolate",
  strata_col = "field",
  fct = drc::LL.3()
)

ec50_estimates(fit)
ec50_metadata(fit)
curve_data(fit)

}
